# Project Doc System Builder Templates

These templates are portable source templates used by `project-doc-system-builder` scripts.

- Copy baseline templates into `docs/_templates/` during bootstrap.
- Use `add-doc.mjs` to create new project docs from these templates.
- Keep project-specific facts out of these templates.
